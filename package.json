{
  "name": "atlas-javascript",
  "version": "0.0.0",
  "private": true,
  "description": "Atlas Authorization — official JavaScript/TypeScript SDKs (@atlasauth/*)",
  "license": "MIT",
  "packageManager": "pnpm@9.15.0",
  "scripts": {
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "typescript": "^5.6.3",
    "vitest": "^2.1.4"
  }
}
